export default function ProgressTimeline({ items }) {
  return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-xl font-bold text-slate-900">Progress timeline</h2>{items.length ? <ol className="mt-5 space-y-4 border-l-2 border-slate-200 pl-5">{items.slice().reverse().slice(0, 5).map((item) => <li key={item.interviewId} className="relative"><span className="absolute -left-[27px] top-1 size-3 rounded-full bg-brand-600 ring-4 ring-white" /><div className="flex justify-between gap-3"><div><p className="font-semibold text-slate-800">{item.role}</p><time className="text-xs text-slate-500">{new Intl.DateTimeFormat(undefined, { dateStyle: 'medium' }).format(new Date(item.date))}</time></div><span className="font-bold text-brand-600">{item.overallScore}/100</span></div></li>)}</ol> : <p className="mt-4 text-sm text-slate-500">Your evaluated interviews will form a timeline here.</p>}</article>;
}
