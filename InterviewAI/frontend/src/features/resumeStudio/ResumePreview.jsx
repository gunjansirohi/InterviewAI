export default function ResumePreview({ resume }) {
  const modern = resume.template === 'modern';
  const minimal = resume.template === 'minimal';
  return <article className={`min-h-[900px] bg-white p-8 shadow-lg ${modern ? 'border-t-8 border-brand-600' : minimal ? 'font-sans' : 'border-t-4 border-slate-800'}`}><header className={modern ? 'text-center' : ''}><h1 className={`font-bold ${minimal ? 'text-2xl' : 'text-3xl'} ${modern ? 'text-brand-700' : 'text-slate-900'}`}>{resume.personalInfo.name || 'Your Name'}</h1><p className="mt-2 text-xs text-slate-500">{Object.values(resume.personalInfo).slice(1).filter(Boolean).join('  •  ')}</p></header><PreviewSection title="Summary" visible={resume.summary}><p>{resume.summary}</p></PreviewSection><PreviewSection title="Skills" visible={resume.skills.length}><p>{resume.skills.join(' • ')}</p></PreviewSection><PreviewSection title="Experience" visible={resume.experience.length}>{resume.experience.map((item, index) => <div key={index} className="mb-4"><div className="flex justify-between gap-4"><h3 className="font-bold">{item.role}{item.company ? ` — ${item.company}` : ''}</h3><span className="text-xs text-slate-500">{[item.startDate, item.endDate].filter(Boolean).join(' – ')}</span></div><ul className="mt-1 list-disc pl-5">{item.descriptions.map((line) => <li key={line}>{line}</li>)}</ul></div>)}</PreviewSection><PreviewSection title="Projects" visible={resume.projects.length}>{resume.projects.map((item, index) => <div key={index} className="mb-3"><h3 className="font-bold">{item.name}</h3><p>{item.description}</p><p className="text-xs text-slate-500">{item.technologies.join(', ')}</p></div>)}</PreviewSection><PreviewSection title="Education" visible={resume.education.length}>{resume.education.map((item, index) => <div key={index} className="mb-2"><h3 className="font-bold">{[item.degree, item.field].filter(Boolean).join(' in ')}</h3><p>{[item.institution, item.startDate, item.endDate].filter(Boolean).join(' • ')}</p></div>)}</PreviewSection><PreviewSection title="Certifications" visible={resume.certifications.length}><p>{resume.certifications.join(' • ')}</p></PreviewSection><PreviewSection title="Languages" visible={resume.languages.length}><p>{resume.languages.join(' • ')}</p></PreviewSection></article>;
}

function PreviewSection({ title, visible, children }) { if (!visible) return null; return <section className="mt-6 text-sm leading-6 text-slate-700"><h2 className="mb-2 border-b border-slate-300 pb-1 text-sm font-bold uppercase tracking-wider text-slate-900">{title}</h2>{children}</section>; }
