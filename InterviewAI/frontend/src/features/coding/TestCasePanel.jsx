export default function TestCasePanel({ testCases, input, onInputChange }) {
  return <section><h3 className="text-sm font-semibold uppercase tracking-wide text-slate-500">Test cases</h3><div className="mt-3 flex gap-2 overflow-x-auto">{testCases.map((test, index) => <button key={index} type="button" onClick={() => onInputChange(test.input)} className="whitespace-nowrap rounded-lg border border-slate-300 px-3 py-2 text-sm font-medium hover:border-brand-600">Example {index + 1}</button>)}</div><label className="mt-3 block text-sm font-medium text-slate-600">Custom input<textarea value={input} onChange={(event) => onInputChange(event.target.value)} rows={3} className="mt-1 w-full rounded-lg border border-slate-300 bg-slate-950 px-3 py-2 font-mono text-sm text-slate-100" /></label></section>;
}
