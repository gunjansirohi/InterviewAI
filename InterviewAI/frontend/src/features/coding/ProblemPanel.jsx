export default function ProblemPanel({ interview }) {
  const { problem } = interview;
  return <section className="h-full overflow-y-auto p-5"><div className="flex flex-wrap gap-2 text-xs font-semibold uppercase"><span className="rounded-full bg-brand-50 px-3 py-1 text-brand-700">{interview.difficulty}</span><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-600">{interview.topic}</span></div><h1 className="mt-4 text-2xl font-bold text-slate-900">{problem.title}</h1><p className="mt-4 whitespace-pre-wrap leading-7 text-slate-700">{problem.statement}</p><h2 className="mt-7 font-bold text-slate-900">Examples</h2><div className="mt-3 space-y-4">{problem.examples.map((example, index) => <div key={index} className="rounded-xl bg-slate-100 p-4 font-mono text-sm"><p><strong>Input:</strong> {example.input}</p><p className="mt-1"><strong>Output:</strong> {example.output}</p>{example.explanation && <p className="mt-2 font-sans text-slate-600">{example.explanation}</p>}</div>)}</div><h2 className="mt-7 font-bold text-slate-900">Constraints</h2><ul className="mt-3 list-disc space-y-1 pl-5 text-sm text-slate-600">{problem.constraints.map((constraint) => <li key={constraint}>{constraint}</li>)}</ul></section>;
}
